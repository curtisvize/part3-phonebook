{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "Part 3 phonebook exercises",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd frontend && npm run build && cp -r dist ../",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"ui build\" && git push",
    "lint": "eslint ."
  },
  "author": "Curtis Vize",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.0.1",
    "express": "^5.1.0",
    "mongoose": "^8.16.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.1",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "eslint": "^9.30.1",
    "globals": "^16.3.0"
  }
}
